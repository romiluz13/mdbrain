# Grove GLM 5.3 — Round 2 debate positions

P1 — Shell connectors: **Conceding deletion; deferring Obsidian ingest.** Kimi/Sol are right that deletion is strictly safer than my typed-error gate: with zero app imports, nobody can construct the stubs, so fake-success becomes impossible rather than merely loud. My gate preserved a surface nobody reaches. But Sol's Workstream B (full Obsidian ingest + `wiki:sync:obsidian` CLI + idempotency tests) is multi-day feature work answering a finding about *honesty*, not missing features. The honest fix is: delete 5 stubs + configs + tests, keep `SourceConnector` contract + registry + Obsidian (labeled "discovery + path-contained export, library API only; ingest is roadmap"). Obsidian ingest lands post-sale. Resolution I can live with: deletion now, ingest deferred.

P2 — mdbrain-export.ts: **Conceding deletion.** Sol's fact settles it: the header promises "export every memory" and the code only signs caller-supplied bundles — there is no retrieval path, so re-exporting it would *manufacture* a public surface whose advertised guarantee is false, which is exactly the failure mode this whole remediation exists to remove. Delete file + test, one CHANGELOG line marking it for post-sale revival with a real retrieval path.

P3 — Maintenance surface: **CLI only.** Kimi's HTTP routes demo well, but every new route is new auth surface, docs, and claim-inventory entries before a sale. The DD findings never said "maintenance lacks HTTP exposure"; they said it has zero production callers. A CLI gives it real callers without touching the API surface or the corrected "30 tools / N routes" claims. Kimi's 501-when-unconfigured pattern is good design — for post-sale. Resolution: `scripts/maintenance-run.ts` CLI now; HTTP routes on the buyer's roadmap.

P4 — Quickstart stack: **Preview stack, Memongo documented honestly, image publication non-blocking.** Sol's ${VOYAGE_API_KEY:?} required interpolation is cheap and correct — adopt it in every compose stack that needs embeddings. But making canonical the `compose.full.yml` that builds Memongo from a *sibling checkout*, gated on first publishing a public OCI image from the *other* repository, blocks this repo's release on external infrastructure work. That is overreach for pre-sale remediation. Resolution: quickstart = preview compose (self-contained MongoDB+Search); README then documents the full-stack path via `compose.full.yml` (sibling checkout stated plainly as a prerequisite) with Memongo image publication tracked as a parallel, non-blocking work item.

P5 — Dreamer without LLM: **Switching to Sol's side: fail closed.** I argued for a documented heuristic fallback, but DD finding F6 *is* the whole-event/0.7 path — keeping it, even labeled "degraded mode", preserves the exact behavior we were cited for. An operator-triggered CLI that errors with "MDBRAIN_LLM_* not configured" is more honest than quietly emitting junk claims. Resolution: Dreamer requires the LLM adapter, fails closed; any heuristic importer is a separate, explicitly named command, deferred post-sale.

P6 — /ready probe: **Middle ground.** Keep the current fail-closed probe semantics, but adopt Sol's cheap half: `ensureWikiSearchIndexes` reports per-index (vector vs text) creation status, and a missing/invalid VOYAGE key surfaces as an actionable, non-secret diagnostic in logs and the readiness body. Full bounded-hybrid-query probing is post-sale hardening — it adds failure modes (embedding round-trip latency, key-tier rate limits) to the one endpoint evaluators hit first. Note for Sol: with the key now *required* by quickstart interpolation, your keyless-evaluator concern mostly evaporates, but the minimal/transactions-only stack must keep its documented failing-ready behavior.

P7 — Claim-drift CI gates: **Adopt, scoped.** The 34-vs-30 drift is a DD finding; a mechanical gate prevents recurrence at near-zero cost. SDK inventory (`Object.keys(createMdbrainTools())` = 14) is a pure unit test. MCP `tools/list` count can be asserted in-process via the stdio server without secrets. Adopt both; README counts reference the tested numbers.

P8 — Image pinning: **Tag for the dev quickstart.** The Docker Hub page documents `preview` as the supported way to get mongot+auto-embed; a dev stack is not a production supply chain, and digest pinning adds a monthly refresh chore this pre-sale pass doesn't need. Digest pinning goes in the same non-blocking track as Memongo image publication (it becomes natural there). 

P9 — tmp/ .gitignore: **No objection.** Agree with Kimi.

Top-3 must-haves for the final plan:
1. The verbatim quickstart (preview stack + required VOYAGE key + honest Memongo path) actually runs end-to-end on a clean machine — this is the deal-killer and nothing outranks it.
2. No fake-success surface survives anywhere: 5 stub connectors deleted, Dreamer fails closed without LLM, mdbrain-export deleted, all counts mechanically gated in CI.
3. Scope discipline: no feature-building (Obsidian ingest, HTTP maintenance routes, image publication, hybrid probing) smuggled into a remediation whose point is that claims match code.
